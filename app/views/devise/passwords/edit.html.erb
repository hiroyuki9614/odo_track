<main id="forgot_password">
  <div class="content mb-3">
    <h2>パスワードの再設定</h2>
    <br>
    <p>パスワードを再設定します。<br>
      下記、フォームにパスワードをご入力下さい。</p>
  </div>
  <div id="form" class="float-3">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <%= f.hidden_field :reset_password_token %>
      <div class="col-4 form-outline mb-4">
        <%= f.label :password, "パスワード", class: "form-label" %>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> 文字以上でご入力下さい)</em><br />
        <% end %>
        <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: "form-control" %>
      </div>
      <div class="col-4 form-outline mb-4">
        <%= f.label :password_confirmation, "パスワード(確認)", class: "form-label" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
      </div>
      <div class="actions">
        <%= f.submit "パスワードを変更する", class:"btn btn-outline-secondary btn-block" %>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</main>
